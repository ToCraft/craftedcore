architectury {
    common(rootProject.enabled_platforms.split(","))
}

//afterEvaluate {
//    remapJar {
//        remapAccessWidener = false
//    }
//}

configurations {
    dev
}

artifacts {
    dev(jar)
}
